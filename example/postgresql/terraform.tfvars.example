# ============================================================================
# Default Configuration for PostgreSQL Cluster
# ============================================================================
# Copy this file to terraform.tfvars and modify as needed
# DO NOT commit terraform.tfvars with sensitive information

# API Configuration (use environment variables or separate tfvars for secrets)
# api_key    = "your_api_key"
# api_secret = "your_api_secret"
# admin_api_key    = "your_admin_api_key"
# admin_api_secret = "your_admin_api_secret"

api_url = "https://kb-cloud-apiserver-endpoint.com/api"

# Cluster Basic Configuration
cluster_name     = "my-pg"
display_name     = "My PostgreSQL Cluster"
org_name         = "default-org"
environment_name = "prod"
project          = "kubeblocks-cloud-ns"

# Engine Configuration
engine       = "postgresql"
engine_version = "18.1.0"
mode         = "replication"  # or "standalone"
cluster_type = "Normal"

# Network and Availability
single_zone        = true
termination_policy = "Delete"

# Maintenance Window
maintenance_start_hour = 18
maintenance_end_hour   = 22
maintenance_weekdays   = "1,2,3,4,5,6,7"

# Component Configuration
component_name = "postgresql"
replicas       = 2
storage_size_gb = 20
storage_class  = "apelocal-rawdisk-xfs"
class_code     = "postgresql.replication.postgresql.1c2g.general"

# I/O Configuration
read_iops  = 2000
write_iops = 2000

# Parameter Template
param_tpl_name      = "postgresql-18.0-default-parameter-template"
param_tpl_partition = "default"

# Backup Configuration
auto_backup_enabled       = true
auto_backup_method        = "wal-g"
backup_repo               = "my-backuprepo"
retention_period          = "7d"
retention_policy          = "LastOne"
backup_schedule           = "0 18 * * *"
snapshot_volumes          = false
pitr_enabled              = true
continuous_backup_method  = "wal-g-archive"
incremental_backup_enabled = false
incremental_backup_schedule = "0 18 * * *"
